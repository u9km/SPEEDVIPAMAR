name: Build Tweak

on: [push]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          echo "THEOS=~/theos" >> $GITHUB_ENV

      - name: Download iOS SDKs (Fix Error)
        run: |
          # هذه الخطوة تحل المشكلة بتحميل الـ SDK يدوياً
          git clone --depth 1 https://github.com/theos/sdks.git ~/theos/sdks
          # مسح المجلدات الفارغة لتجنب الأخطاء
          rm -rf ~/theos/sdks/.git

      - name: Download fishhook (Required)
        run: |
          # تحميل ملفات fishhook المطلوبة للكود الخاص بك
          curl -o fishhook.c https://raw.githubusercontent.com/facebook/fishhook/master/fishhook.c
          curl -o fishhook.h https://raw.githubusercontent.com/facebook/fishhook/master/fishhook.h

      - name: Build Tweak
        run: |
          # بناء الملف وتجاهل الأخطاء البسيطة
          make package FINALPACKAGE=1

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: AntiCheatShield
          path: packages/*.deb
